# XFCE 图形验证与 GT9886 触摸支持（2026-09-05）

## 结果与边界

XFCE 桌面已在手机屏幕显示，用户确认安装成功。Xorg 使用 DSI-1，
分辨率 1080×2340、60 Hz；Mesa renderer 为 freedreno FD640，
`direct rendering: Yes`、`Accelerated: yes`。

用户随后反馈触摸不可用。运行中的控制 DTB 声明 `goodix,gt9886`，
I²C 设备 `1-005d` 存在但没有驱动绑定；原模块的 OF alias 不匹配它。
补入 GTX8 驱动后，产品 ID 检查和 probe 通过，新增
`Goodix GTX8 Capacitive TouchScreen`（`/dev/input/event3`），udev 标记
`ID_INPUT_TOUCHSCREEN=1`，Xorg 已用 libinput 接入。

新内核 Build ID `223bccaf12ca6e9bf8ccadefa3ee8d2cd1136cda` 已正常启动；
GTX8 由内核树内模块自动绑定，启动时 taint 为 0。IRQ 模式下用户滑动后的
70 秒采集为 0 input 事件，IRQ 174/GPIO122 计数仍为 0。`0x4100` 的只读帧在
滑动期间出现有效、变化的单触点坐标，证实控制器扫描和 I²C 读取可用，不能替代
Linux IRQ 投递或 input 事件通过。

一次按驱动语义的坐标状态 ACK、GPIO122 上拉/16 mA 加下降沿，以及该 pin 状态下
reset54 低 2 ms/高后等待 100 ms，均未恢复 IRQ 事件。域链记录为 msmgpio hwirq 122
→ PDC 90 → GICv3 602；helper 已卸载并恢复 GPIO122/IRQ174 的 pull-down/2 mA、
level-low。

临时外部 GTX8 模块以 16 ms 主动轮询同一读取、校验和上报路径后，用户确认触摸
可以操作桌面，故物理触摸状态为 **PASS_USER_CONFIRMED_POLLING**。运行中仍是
`ebadafc3e01cb`，此结果不表示 IRQ 根因已修复。最新 60 秒计数窗口为 0，未取得
可报告的按下、释放、坐标或多点自动采集结果。
不能用 input 节点、原始坐标帧或桌面接入替代点按、滑动、释放、坐标方向及多点验收。

## 桌面安装

手机使用 Debian 13 trixie。安装前模拟 APT 事务，确认没有移除包或更新
Linux/GRUB/U-Boot。此次未修改内核启动参数或系统 APT 源。

可在网络正常的手机上复现安装：

```sh
sudo apt-get update
sudo apt-get install --no-install-recommends \
  xfce4 xfce4-terminal lightdm lightdm-gtk-greeter \
  xserver-xorg-core xserver-xorg-input-libinput libgl1-mesa-dri \
  mesa-utils dbus-x11 dbus-user-session fonts-noto-cjk \
  xauth x11-utils scrot librsvg2-common
```

本次 NCM 没有默认互联网路由，通过 SSH 的远端动态转发临时提供
`127.0.0.1:18080` SOCKS5，APT 用 `socks5h` 远端解析 DNS。
代理只在命令期间存在，TLS 和签名检查保留。安装后已清理 APT 下载缓存，
从 188 MiB 降至 24 KiB。软件包本身保留在 userdata。

版本：XFCE 元包 4.20.1、Xorg 21.1.16、LightDM 1.32.0、
Mesa 25.0.7。XFCE 与 SVG loader 共新增 260 个包。

首次显示使用 `/run/raphael-desktop/lightdm.conf` 和 LightDM 的运行期
systemd drop-in，自动进入 `raphael` 的 XFCE 测试会话。此配置重启后消失；
LightDM 服务本身已启用，下次正常启动显示登录界面。没有配置永久免密登录。

已安装 Onboard 1.4.1-10 及其依赖，共 27 个新包，无升级或删除；其中补齐
`gsettings-desktop-schemas`、`at-spi2-core`、`gir1.2-atspi-2.0`。密码登录保持启用；
Onboard 已配置为 greeter 的 `--xid` 屏幕键盘，并在 `lijian` 的 XFCE 会话中自动显示和启动。
Gio 设置为 auto-show、docking、start-minimized 和 icon-palette 启用，且因手机未报告
`SW_TABLET_MODE` 而关闭 tablet-mode detection。当前 boot `52483143-2111-4000-bd28-8d7f9ccb524b`
运行 47 分钟，`lijian` seat0 会话、`xfce4-session`、Onboard 均可见，失败服务为 0。
未在本记录保存或引用任何密码。

## 触摸源码与构建

内核本地提交：

- `ede46f66a`：导入 GTX8 驱动、Kconfig/Makefile 及绑定。
- `ebadafc3e01cb8940509e93dfe670e07bd2e13ca`：开发配置启用模块，
  Raphael 候选 DTS 补齐触摸节点及供电。
- `61192118e44cd9618d4b8114f76266df8581d28f`：加入可选 GTX8 bring-up
  轮询；默认 `poll_interval_ms=0` 仍使用 IRQ，非零参数受限于 10–1000 ms。

来源为 `GavinLiuOnline/xiaomi_raphael_kernel` 的
`c526b7bf7ebc3fbfee244be252a2c1bd061ca749`，与运行中的控制 DTB 同源。
保留 Goodix、Linaro、Jens Reidel 的版权及许可证；C 文件补充了直接使用
的 delay、regulator 头文件。驱动使用芯片现有固件，没有固件烧写功能。

接线来自 c526：I²C19 (`0xc88000`) / `0x5d` / 100 kHz，TLMM122
低电平中断、TLMM54 低有效 reset，TLMM98 控制 3.3 V，TLMM100 控制
1.8 V。范围是 1080×2340。未将其他 Goodix 协议强制绑定到本机。

主机完整内核、模块与候选 DTB 构建通过，914 个模块的 ABI、initramfs
容量、FAT 文件回读和 Fastboot 包预检通过；新 initramfs 包含
`goodix_gtx8.ko`，模块标记 `intree: Y`。
驱动 C/H 的 checkpatch 为 0 errors、0 warnings。提交级检查仍提示
DCO 签署缺失、说明换行、来源提交不在本树、MAINTAINERS 和 binding
拆分事项；当前为开发移植，未标为上游可投稿补丁，没有代签 Signed-off-by。
本机未安装 dt-schema，未运行 binding schema 检查。GT9896 和挂起恢复未验收。

## 手机当前部署

运行中的 `ebadafc3e01cb` / Build ID `223bccaf12ca6e9bf8ccadefa3ee8d2cd1136cda`
仍使用临时外部 16 ms 轮询模块；此前树内 IRQ 模式的自动绑定和 taint 0 事实保留。

`61192118e44cd9618d4b8114f76266df8581d28f` 已完整构建、打包并在线安装到
`/boot/raphael-61192118/`，GRUB 已切换，`/boot` 已恢复只读。其 Build ID 为
`624d1f332723d399e2b95a584d77b8e7c5da8faa`，**尚未启动验证**；不要将当前临时轮询
的用户确认归于该内核。

| 文件 | SHA-256 |
| --- | --- |
| 6119 linux.efi | `a08501b3b953ecd32c9f8ce2c5769d23d9d3d4156858165fc803cdb81a2d8d14` |
| 6119 initramfs | `7496ef83c2bae5e558f4c98953ff1440926a22a72b68bbd7766ba9e24050ae5c` |
| 已切换的 grub.cfg | `750c6be64a50c7945f1d84de33e12f6cf36bd79f29c0a6d5f4806eefa50787c0` |
| `gt9886-polling.conf` | `8c25ed0e75fac0faba8bc016313b5beefb41ec7ae3878c2c15133ff3dee8ebef` |

默认配置包携带 `config/raphael/gt9886-polling.conf`，只为 GTX8 模块指定
`poll_interval_ms=16`；移除此选项即回到默认 IRQ 路径。旧 GRUB 备份位于
`/var/backups/raphael/boot-before-polling-20260905/`。此处为 cache 文件更新，
不是第二次完整 Fastboot 刷写。

实机继续使用 c526 控制 DTB；新源码候选 DTS 未作为运行 DTB 验证。
## 本次桌面期间的关机

安装后的两次启动切换均有明确日志：`Power key pressed short.`，随后
`Powering off...`、`poweroff.target`、正常停服务。它们发生在测试触摸
模块加载之前，说明这两次断连由系统对电源键短按的关机策略触发。
这不能解释或替代更早的 7.3 启动故障诊断。

原始安装日志、截图、输入采集、构建和部署记录位于本地忽略目录
`artifacts/device-private/20260905-desktop/`。结构化结果见
`logs/raphael/2026-09-05-xfce-gt9886-summary.json`。

下一次验收先确认 6119 的新 Build ID、树内轮询参数和自动绑定；IRQ 路径、
按下/释放、坐标与多点自动采集仍为独立验收项。
